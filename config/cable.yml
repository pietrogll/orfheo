# Action Cable configuration for WebSocket connections

# Redis configuration for Action Cable
production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: orfheo_production

development:
  adapter: redis
  url: redis://localhost:6379/1

test:
  adapter: test
